# Supabase — copy this file to .env and fill in your project values.
# Expo only exposes variables prefixed with EXPO_PUBLIC_ to the app bundle.
# Find these in your Supabase dashboard: Project Settings → API.

# Project URL, e.g. https://abcdefghijklmnop.supabase.co
EXPO_PUBLIC_SUPABASE_URL=

# Client key. Use the new "publishable" key (sb_publishable_...) if your project
# has the new API key system, otherwise the legacy "anon" key. Either name works.
EXPO_PUBLIC_SUPABASE_PUBLISHABLE_KEY=
# EXPO_PUBLIC_SUPABASE_ANON_KEY=

# Test-only: service-role key (Project Settings -> API -> service_role).
# Used ONLY by the security suite (pnpm test:security) to create and delete test
# users/households. NEVER import this into app code — the app uses the anon key
# only. Leave blank to run the security suite in anon-fallback mode (leaves data).
SUPABASE_SERVICE_ROLE_KEY=
