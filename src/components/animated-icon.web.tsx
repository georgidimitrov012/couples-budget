// Web has no native splash screen to hand off from, so there is nothing to
// cover: the platform-specific no-op keeps `_layout.tsx` free of Platform checks.
export function AnimatedSplashOverlay() {
  return null;
}
